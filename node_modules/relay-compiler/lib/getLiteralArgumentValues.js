/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 *  strict
 * @format
 */
'use strict';

function getLiteralArgumentValues(args) {
  var values = {};
  args.forEach(function (arg) {
    !(arg.value.kind === 'Literal') ? process.env.NODE_ENV !== "production" ? require("fbjs/lib/invariant")(false, 'getLiteralArgumentValues(): Expected all args to be literals.') : require("fbjs/lib/invariant")(false) : void 0;
    values[arg.name] = arg.value.value;
  });
  return values;
}

module.exports = getLiteralArgumentValues;