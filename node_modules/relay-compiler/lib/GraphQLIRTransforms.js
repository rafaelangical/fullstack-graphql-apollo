/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 *  strict-local
 * @format
 */
'use strict';

// Transforms applied to fragments used for reading data from a store
var FRAGMENT_TRANSFORMS = [require("./FlattenTransform").transformWithOptions({
  flattenAbstractTypes: true
}), require("./SkipRedundantNodesTransform").transform]; // Transforms applied to queries/mutations/subscriptions that are used for
// fetching data from the server and parsing those responses.

var QUERY_TRANSFORMS = [require("./SkipClientFieldTransform").transform, require("./SkipUnreachableNodeTransform").transform]; // Transforms applied to the code used to process a query response.

var CODEGEN_TRANSFORMS = [require("./InlineFragmentsTransform").transform, require("./FlattenTransform").transformWithOptions({
  flattenAbstractTypes: true
}), require("./SkipRedundantNodesTransform").transform, require("./FilterDirectivesTransform").transform];
module.exports = {
  codegenTransforms: CODEGEN_TRANSFORMS,
  fragmentTransforms: FRAGMENT_TRANSFORMS,
  queryTransforms: QUERY_TRANSFORMS
};