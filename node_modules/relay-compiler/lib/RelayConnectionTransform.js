/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 * @format
 */
'use strict';

var _objectSpread2 = require("@babel/runtime/helpers/interopRequireDefault")(require("@babel/runtime/helpers/objectSpread"));

var _toConsumableArray2 = require("@babel/runtime/helpers/interopRequireDefault")(require("@babel/runtime/helpers/toConsumableArray"));

var CONNECTION = 'connection';
var HANDLER = 'handler';
/**
 * @public
 *
 * Transforms fields with the `@connection` directive:
 * - Verifies that the field type is connection-like.
 * - Adds a `handle` property to the field, either the user-provided `handle`
 *   argument or the default value "connection".
 * - Inserts a sub-fragment on the field to ensure that standard connection
 *   fields are fetched (e.g. cursors, node ids, page info).
 */

function relayConnectionTransform(context) {
  return require("./GraphQLIRTransformer").transform(context, {
    Fragment: visitFragmentOrRoot,
    LinkedField: visitLinkedOrMatchField,
    MatchField: visitLinkedOrMatchField,
    Root: visitFragmentOrRoot
  }, function (node) {
    return {
      path: [],
      connectionMetadata: [],
      definitionName: node.name
    };
  });
}

var SCHEMA_EXTENSION = 'directive @connection(key: String!, filters: [String], handler: String) on FIELD';
/**
 * @internal
 */

function visitFragmentOrRoot(node, options) {
  var transformedNode = this.traverse(node, options);
  var connectionMetadata = options.connectionMetadata;

  if (connectionMetadata.length) {
    return (0, _objectSpread2["default"])({}, transformedNode, {
      metadata: (0, _objectSpread2["default"])({}, transformedNode.metadata, {
        connection: connectionMetadata
      })
    });
  }

  return transformedNode;
}
/**
 * @internal
 */


function visitLinkedOrMatchField(field, options) {
  var _handler;

  var isPlural = require("./GraphQLSchemaUtils").getNullableType(field.type) instanceof require("graphql").GraphQLList;

  options.path.push(isPlural ? null : field.alias || field.name);
  var transformedField = this.traverse(field, options);
  var connectionDirective = field.directives.find(function (directive) {
    return directive.name === CONNECTION;
  });

  if (!connectionDirective) {
    options.path.pop();
    return transformedField;
  }

  var definitionName = options.definitionName;
  validateConnectionSelection(definitionName, transformedField);
  validateConnectionType(definitionName, transformedField);
  var pathHasPlural = options.path.includes(null);
  var firstArg = findArg(transformedField, require("./RelayConnectionConstants").FIRST);
  var lastArg = findArg(transformedField, require("./RelayConnectionConstants").LAST);
  var direction = null;
  var countArg = null;
  var cursorArg = null;

  if (firstArg && !lastArg) {
    direction = 'forward';
    countArg = firstArg;
    cursorArg = findArg(transformedField, require("./RelayConnectionConstants").AFTER);
  } else if (lastArg && !firstArg) {
    direction = 'backward';
    countArg = lastArg;
    cursorArg = findArg(transformedField, require("./RelayConnectionConstants").BEFORE);
  } else if (lastArg && firstArg) {
    direction = 'bidirectional'; // TODO(T26511885) Maybe add connection metadata to this case
  }

  var countVariable = countArg && countArg.value.kind === 'Variable' ? countArg.value.variableName : null;
  var cursorVariable = cursorArg && cursorArg.value.kind === 'Variable' ? cursorArg.value.variableName : null;
  options.connectionMetadata.push({
    count: countVariable,
    cursor: cursorVariable,
    direction: direction,
    path: pathHasPlural ? null : (0, _toConsumableArray2["default"])(options.path)
  });
  options.path.pop();

  var _getLiteralArgumentVa = require("./getLiteralArgumentValues")(connectionDirective.args),
      handler = _getLiteralArgumentVa.handler,
      key = _getLiteralArgumentVa.key,
      filters = _getLiteralArgumentVa.filters;

  if (handler != null && typeof handler !== 'string') {
    var _ref, _handleArg$value;

    var handleArg = connectionDirective.args.find(function (arg) {
      return arg.name === 'key';
    });
    throw require("./RelayCompilerError").createUserError("Expected the ".concat(HANDLER, " argument to ") + "@".concat(CONNECTION, " to be a string literal for field ").concat(field.name, "."), [(_ref = handleArg === null || handleArg === void 0 ? void 0 : (_handleArg$value = handleArg.value) === null || _handleArg$value === void 0 ? void 0 : _handleArg$value.loc) !== null && _ref !== void 0 ? _ref : connectionDirective.loc]);
  }

  if (typeof key !== 'string') {
    var _ref2, _keyArg$value;

    var keyArg = connectionDirective.args.find(function (arg) {
      return arg.name === 'key';
    });
    throw require("./RelayCompilerError").createUserError("Expected the ".concat(require("./RelayConnectionConstants").KEY, " argument to ") + "@".concat(CONNECTION, " to be a string literal for field ").concat(field.name, "."), [(_ref2 = keyArg === null || keyArg === void 0 ? void 0 : (_keyArg$value = keyArg.value) === null || _keyArg$value === void 0 ? void 0 : _keyArg$value.loc) !== null && _ref2 !== void 0 ? _ref2 : connectionDirective.loc]);
  }

  var postfix = field.alias || field.name;

  if (!key.endsWith('_' + postfix)) {
    var _ref3, _keyArg$value2;

    var _keyArg = connectionDirective.args.find(function (arg) {
      return arg.name === 'key';
    });

    throw require("./RelayCompilerError").createUserError("Expected the ".concat(require("./RelayConnectionConstants").KEY, " argument to ") + "@".concat(CONNECTION, " to be of form <SomeName>_").concat(postfix, ", got '").concat(key, "'. ") + 'For detailed explanation, check out ' + 'https://facebook.github.io/relay/docs/en/pagination-container.html#connection', [(_ref3 = _keyArg === null || _keyArg === void 0 ? void 0 : (_keyArg$value2 = _keyArg.value) === null || _keyArg$value2 === void 0 ? void 0 : _keyArg$value2.loc) !== null && _ref3 !== void 0 ? _ref3 : connectionDirective.loc]);
  }

  var generateFilters = function generateFilters() {
    var filteredVariableArgs = field.args.filter(function (arg) {
      return !require("relay-runtime").ConnectionInterface.isConnectionCall({
        name: arg.name,
        value: null
      });
    }).map(function (arg) {
      return arg.name;
    });
    return filteredVariableArgs.length === 0 ? null : filteredVariableArgs;
  };

  var handle = {
    name: (_handler = handler) !== null && _handler !== void 0 ? _handler : CONNECTION,
    key: key,
    filters: filters || generateFilters()
  };

  if (direction !== null) {
    var fragment = generateConnectionFragment(this.getContext(), transformedField.loc, transformedField.type, direction);
    transformedField = (0, _objectSpread2["default"])({}, transformedField, {
      selections: transformedField.selections.concat(fragment)
    });
  }

  return (0, _objectSpread2["default"])({}, transformedField, {
    directives: transformedField.directives.filter(function (directive) {
      return directive.name !== CONNECTION;
    }),
    handles: transformedField.handles ? (0, _toConsumableArray2["default"])(transformedField.handles).concat([handle]) : [handle]
  });
}
/**
 * @internal
 *
 * Generates a fragment on the given type that fetches the minimal connection
 * fields in order to merge different pagination results together at runtime.
 */


function generateConnectionFragment(context, loc, type, direction) {
  var _ConnectionInterface$ = require("relay-runtime").ConnectionInterface.get(),
      CURSOR = _ConnectionInterface$.CURSOR,
      EDGES = _ConnectionInterface$.EDGES,
      END_CURSOR = _ConnectionInterface$.END_CURSOR,
      HAS_NEXT_PAGE = _ConnectionInterface$.HAS_NEXT_PAGE,
      HAS_PREV_PAGE = _ConnectionInterface$.HAS_PREV_PAGE,
      NODE = _ConnectionInterface$.NODE,
      PAGE_INFO = _ConnectionInterface$.PAGE_INFO,
      START_CURSOR = _ConnectionInterface$.START_CURSOR;

  var compositeType = require("graphql").assertCompositeType(require("./GraphQLSchemaUtils").getNullableType(type));

  var pageInfo = PAGE_INFO;

  if (direction === 'forward') {
    pageInfo += "{\n      ".concat(END_CURSOR, "\n      ").concat(HAS_NEXT_PAGE, "\n    }");
  } else if (direction === 'backward') {
    pageInfo += "{\n      ".concat(HAS_PREV_PAGE, "\n      ").concat(START_CURSOR, "\n    }");
  } else {
    pageInfo += "{\n      ".concat(END_CURSOR, "\n      ").concat(HAS_NEXT_PAGE, "\n      ").concat(HAS_PREV_PAGE, "\n      ").concat(START_CURSOR, "\n    }");
  }

  var fragmentString = "fragment ConnectionFragment on ".concat(String(compositeType), " {\n      ").concat(EDGES, " {\n        ").concat(CURSOR, "\n        ").concat(NODE, " {\n          __typename # rely on GenerateRequisiteFieldTransform to add \"id\"\n        }\n      }\n      ").concat(pageInfo, "\n    }");

  var ast = require("graphql").parse(fragmentString);

  var fragmentAST = ast.definitions[0];

  if (fragmentAST == null || fragmentAST.kind !== 'FragmentDefinition') {
    throw require("./RelayCompilerError").createCompilerError('RelayConnectionTransform: Expected a fragment definition AST.', null, [fragmentAST].filter(Boolean));
  }

  var fragment = require("./RelayParser").transform(context.clientSchema, [fragmentAST])[0];

  if (fragment == null || fragment.kind !== 'Fragment') {
    throw require("./RelayCompilerError").createCompilerError('RelayConnectionTransform: Expected a connection fragment.', [fragment === null || fragment === void 0 ? void 0 : fragment.loc].filter(Boolean));
  }

  return {
    directives: [],
    kind: 'InlineFragment',
    loc: {
      kind: 'Derived',
      source: loc
    },
    metadata: null,
    selections: fragment.selections,
    typeCondition: compositeType
  };
}

function findArg(field, argName) {
  return field.args && field.args.find(function (arg) {
    return arg.name === argName;
  });
}
/**
 * @internal
 *
 * Validates that the selection is a valid connection:
 * - Specifies a first or last argument to prevent accidental, unconstrained
 *   data access.
 * - Has an `edges` selection, otherwise there is nothing to paginate.
 *
 * TODO: This implementation requires the edges field to be a direct selection
 * and not contained within an inline fragment or fragment spread. It's
 * technically possible to remove this restriction if this pattern becomes
 * common/necessary.
 */


function validateConnectionSelection(definitionName, field) {
  var _ConnectionInterface$2 = require("relay-runtime").ConnectionInterface.get(),
      EDGES = _ConnectionInterface$2.EDGES;

  if (!findArg(field, require("./RelayConnectionConstants").FIRST) && !findArg(field, require("./RelayConnectionConstants").LAST)) {
    throw require("./RelayCompilerError").createUserError("Expected field `".concat(field.name, ": ") + "".concat(String(field.type), "` to have a ").concat(require("./RelayConnectionConstants").FIRST, " or ").concat(require("./RelayConnectionConstants").LAST, " argument in ") + "document `".concat(definitionName, "`."), [field.loc]);
  }

  if (!field.selections.some(function (selection) {
    return selection.kind === 'LinkedField' && selection.name === EDGES;
  })) {
    throw require("./RelayCompilerError").createUserError("Expected field `".concat(field.name, ": ") + "".concat(String(field.type), "` to have a ").concat(EDGES, " selection in document ") + "`".concat(definitionName, "`."), [field.loc]);
  }
}
/**
 * @internal
 *
 * Validates that the type satisfies the Connection specification:
 * - The type has an edges field, and edges have scalar `cursor` and object
 *   `node` fields.
 * - The type has a page info field which is an object with the correct
 *   subfields.
 */


function validateConnectionType(definitionName, field) {
  var type = field.type;

  var _ConnectionInterface$3 = require("relay-runtime").ConnectionInterface.get(),
      CURSOR = _ConnectionInterface$3.CURSOR,
      EDGES = _ConnectionInterface$3.EDGES,
      END_CURSOR = _ConnectionInterface$3.END_CURSOR,
      HAS_NEXT_PAGE = _ConnectionInterface$3.HAS_NEXT_PAGE,
      HAS_PREV_PAGE = _ConnectionInterface$3.HAS_PREV_PAGE,
      NODE = _ConnectionInterface$3.NODE,
      PAGE_INFO = _ConnectionInterface$3.PAGE_INFO,
      START_CURSOR = _ConnectionInterface$3.START_CURSOR;

  var typeWithFields = require("./GraphQLSchemaUtils").assertTypeWithFields(require("./GraphQLSchemaUtils").getNullableType(type));

  var typeFields = typeWithFields.getFields();
  var edges = typeFields[EDGES];

  if (edges == null) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have an '").concat(EDGES, "' field in document '").concat(definitionName, "'."), [field.loc]);
  }

  var edgesType = require("./GraphQLSchemaUtils").getNullableType(edges.type);

  if (!(edgesType instanceof require("graphql").GraphQLList)) {
    throw require("./RelayCompilerError").createUserError("Expected '".concat(EDGES, "' field on type '").concat(String(type), "' to be a list type in document '").concat(definitionName, "'."), [field.loc]);
  }

  var edgeType = require("./GraphQLSchemaUtils").getNullableType(edgesType.ofType);

  if (!(edgeType instanceof require("graphql").GraphQLObjectType)) {
    throw require("./RelayCompilerError").createUserError("Expected '".concat(EDGES, "' field on type '").concat(String(type), "' to be a list of objects in document '").concat(definitionName, "'."), [field.loc]);
  }

  var node = edgeType.getFields()[NODE];

  if (node == null) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have have a '").concat(EDGES, " { ").concat(NODE, " }' field in in document '").concat(definitionName, "'."), [field.loc]);
  }

  var nodeType = require("./GraphQLSchemaUtils").getNullableType(node.type);

  if (!(nodeType instanceof require("graphql").GraphQLInterfaceType || nodeType instanceof require("graphql").GraphQLUnionType || nodeType instanceof require("graphql").GraphQLObjectType)) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have a '").concat(EDGES, " { ").concat(NODE, " }' field for which the type is an interface, object, or union in document '").concat(definitionName, "'."), [field.loc]);
  }

  var cursor = edgeType.getFields()[CURSOR];

  if (cursor == null || !(require("./GraphQLSchemaUtils").getNullableType(cursor.type) instanceof require("graphql").GraphQLScalarType)) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have a '").concat(EDGES, " { ").concat(CURSOR, " }' scalar field in document '").concat(definitionName, "'."), [field.loc]);
  }

  var pageInfo = typeFields[PAGE_INFO];

  if (pageInfo == null) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have a '").concat(EDGES, " { ").concat(PAGE_INFO, " }' field in document '").concat(definitionName, "'."), [field.loc]);
  }

  var pageInfoType = require("./GraphQLSchemaUtils").getNullableType(pageInfo.type);

  if (!(pageInfoType instanceof require("graphql").GraphQLObjectType)) {
    throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(type), "' to have a '").concat(EDGES, " { ").concat(PAGE_INFO, " }' field with object type in document '").concat(definitionName, "'."), [field.loc]);
  }

  [END_CURSOR, HAS_NEXT_PAGE, HAS_PREV_PAGE, START_CURSOR].forEach(function (fieldName) {
    var pageInfoField = pageInfoType.getFields()[fieldName];

    if (pageInfoField == null || !(require("./GraphQLSchemaUtils").getNullableType(pageInfoField.type) instanceof require("graphql").GraphQLScalarType)) {
      throw require("./RelayCompilerError").createUserError("Expected type '".concat(String(pageInfo.type), "' to have a '").concat(fieldName, "' scalar field in document '").concat(definitionName, "'."), [field.loc]);
    }
  });
}

module.exports = {
  CONNECTION: CONNECTION,
  SCHEMA_EXTENSION: SCHEMA_EXTENSION,
  transform: relayConnectionTransform
};