/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 *  strict-local
 * @format
 */
'use strict';

var _toConsumableArray2 = require("@babel/runtime/helpers/interopRequireDefault")(require("@babel/runtime/helpers/toConsumableArray"));

function DisallowIdAsAliasValidationRule(context) {
  return {
    Field: function Field(field) {
      if (field.alias && field.alias.value === 'id' && field.name.value !== 'id') {
        throw new Error('RelayValidator: Relay does not allow aliasing fields to `id`. ' + 'This name is reserved for the globally unique `id` field on ' + '`Node`.');
      }
    }
  };
}

var relayLocalRules = (0, _toConsumableArray2["default"])(require("./GraphQLValidator").LOCAL_RULES).concat([DisallowIdAsAliasValidationRule]);
module.exports = {
  GLOBAL_RULES: require("./GraphQLValidator").GLOBAL_RULES,
  LOCAL_RULES: relayLocalRules,
  validate: require("./GraphQLValidator").validate
};